import styled from 'styled-components';

const Container = styled.div`
  display: flex;
  width: 150px;
  height: 30px;
  align-items: center;
`;

const Menu = () => {
  return <Container>Home</Container>;
};

export default Menu;
